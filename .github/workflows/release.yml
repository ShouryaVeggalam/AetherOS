# Draft GitHub Releases for annotated version tags (no auto-publish)
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Build draft GitHub Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install build tooling
        run: |
          python -m pip install -U pip
          pip install build twine
          pip install -e ".[dev]"

      - name: Quality gate
        run: |
          ruff check .
          black --check .
          python -m compileall -q aetheros services plugins
          python scripts/check_import_cycles.py
          pytest --cov=aetheros --cov-fail-under=90

      - name: Build sdist and wheel
        run: python -m build

      - name: Check distributions
        run: twine check dist/*

      - name: Create DRAFT GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          name: AetherOS ${{ github.ref_name }} — Nexus
          files: |
            dist/*.whl
            dist/*.tar.gz
          body: |
            # AetherOS ${{ github.ref_name }} — Nexus

            Explainable Operating Intelligence Platform · **Observe · Reason · Simulate · Explain**

            This release was created as a **draft**. Human review required before publish.

            ## What's new

            - Plugin SDK · Public API / `AetherClient` · Marketplace · Policy Studio · Enterprise Edition
            - Full notes: [docs/releases/v5.0.0.md](https://github.com/shouryaveggalam/AetherOS/blob/main/docs/releases/v5.0.0.md)

            ## Install

            ```bash
            pip install dist/aetheros-*.whl
            python -m aetheros
            ```

            ## Docs

            - [CHANGELOG.md](https://github.com/shouryaveggalam/AetherOS/blob/main/CHANGELOG.md)
            - [Architecture](https://github.com/shouryaveggalam/AetherOS/blob/main/docs/architecture.md)
            - [Benchmarks](https://github.com/shouryaveggalam/AetherOS/blob/main/docs/benchmarks.md)
            - [SECURITY.md](https://github.com/shouryaveggalam/AetherOS/blob/main/SECURITY.md)

            Recommendation-only. Userspace. MIT License.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
